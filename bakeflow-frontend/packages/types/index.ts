// @bakeflow/types — placeholder entry point.
// Declared as "main" in package.json; without it the package cannot resolve.
// Real exports land when this package is built per docs/FRONTEND-STRUCTURE.md.
export {};
