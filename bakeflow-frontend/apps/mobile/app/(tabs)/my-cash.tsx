import { CashScreen } from '../../features/finance/components/CashScreen';

/** My cash — the Cashier's own till (prototype `my-cash`). */
export default function MyCashTab(): React.JSX.Element {
  return <CashScreen mine />;
}
