import '../global.css';

import { Stack } from 'expo-router';

/**
 * Root layout. Scaffolding only — the operational surfaces defined in
 * docs/FRONTEND-STRUCTURE.md are not built yet. This exists so the bundler,
 * the type checker and the first native build have a valid entry point.
 */
export default function RootLayout() {
  return <Stack />;
}
