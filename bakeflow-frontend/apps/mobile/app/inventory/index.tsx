import { StockScreen } from '../../features/inventory/components/StockScreen';

/** Stock — the prototype's `inventory-monitor`; the branch's default stockroom first. */
export default function StockRoute(): React.JSX.Element {
  return <StockScreen />;
}
